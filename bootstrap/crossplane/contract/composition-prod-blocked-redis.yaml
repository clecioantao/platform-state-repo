apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: redis-prod-blocked
  labels:
    idp.platform.io/provider: kubernetes
    idp.platform.io/env: prod
    idp.platform.io/blocked: "true"
spec:
  compositeTypeRef:
    apiVersion: idp.platform.io/v1alpha1
    kind: XRedisInstance

  mode: Pipeline
  pipeline:
    - step: blocked
      functionRef:
        name: function-patch-and-transform
      input:
        apiVersion: pt.crossplane.io/v1beta1
        kind: Resources
        resources: []
