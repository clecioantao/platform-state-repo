apiVersion: idp.platform.io/v1alpha1
kind: RedisInstance
metadata:
  name: {{appName}}-redis
  namespace: pe-dev
spec:
  parameters:
    app: {{appName}}
    env: dev
    owner: {{owner}}
    size: {{size}}
